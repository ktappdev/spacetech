{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "deploy": "git pull && pnpm install && pnpm run build && pm2 start ecosystem.config.cjs",
    "dev": "VITE_ALLOW_HOST=spacetech.lyricut.com astro dev --host=0.0.0.0 --port=3001 --allowed-hosts=true",
    "build": "astro build",
    "preview": "VITE_ALLOW_HOST=spacetech.lyricut.com astro preview --host=0.0.0.0 --port=3001 --allowed-hosts=true",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.2.5"
  },
  "devDependencies": {
    "vite": "6.0.6"
  }
}
